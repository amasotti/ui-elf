name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  verify:
    name: Verify
    uses: ./.github/workflows/test-lint.yml

  release:
    name: Release
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      matrix:
        goos: [linux, windows, darwin]
        goarch: [amd64, arm64]
        exclude:
          - goos: windows
            goarch: arm64
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'

    - name: Build
      env:
        GOOS: ${{ matrix.goos }}
        GOARCH: ${{ matrix.goarch }}
      run: |
        BINARY_NAME=ui-elf
        if [ "${{ matrix.goos }}" = "windows" ]; then
          BINARY_NAME=ui-elf.exe
        fi
        mkdir -p dist
        go build -o dist/${BINARY_NAME} cmd/ui-elf/main.go
        
        # Package the binary
        cd dist
        if [ "${{ matrix.goos }}" = "windows" ]; then
          zip ui-elf-${{ matrix.goos }}-${{ matrix.goarch }}.zip ${BINARY_NAME}
        else
          tar -czvf ui-elf-${{ matrix.goos }}-${{ matrix.goarch }}.tar.gz ${BINARY_NAME}
        fi

    - name: Release
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: |
          dist/*.tar.gz
          dist/*.zip
